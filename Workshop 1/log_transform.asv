function B= log_transform(A,c)
%Denne funktion anvender intensitetstransformation c*log(1+a_ij) på
%matricen A.
%
%error('Denne funktion er endnu ikke implementeret')
B = arrayfun(logT(A,c),A)

end

function B = logT(A,c)


B = c * log(1+A);
end